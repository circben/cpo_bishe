# 第五章 模型训练与推理

---

## 5.1 训练方案总体设计

本系统的模型训练采用双轨并行策略：CPO路线基于步骤级偏好数据使用DPO算法进行偏好优化微调，TS-SFT路线基于搜索成功路径进行标准监督微调。两条路线使用相同的基座模型、LoRA配置和训练基础设施，仅在训练数据格式和损失函数上存在差异，从而在控制变量的前提下公平比较偏好信号与监督信号对推理增强的相对贡献。

基座模型选用Qwen系列（如Qwen2.5-7B-Instruct），通过`src/cpo/llm/model_loader.py`统一加载，支持本地的模型权重加载。模型可通过`src/cpo/llm/quantization.py`进行4-bit量化以降低显存占用。训练时，通过`src/cpo/train/lora_setup.py`为模型的注意力投影层（Q、K、V、O）和前馈网络层注入LoRA适配器。

训练脚本执行流程图：

```
Stage 2输出(偏好数据/成功路径)
         ↓
  prepare_dpo_data.py / 直接格式化为(prompt, response)
         ↓
  train_dpo.py / train_ts_sft.py
         ↓
  加载基座模型 → 注入LoRA → 构建数据集 → 训练循环 → 保存适配器
         ↓
  LoRA权重文件(.pt) → 供Stage 4评估使用
```

---

## 5.2 CPO训练路线

### 5.2.1 训练数据格式

CPO训练的输入数据由`scripts/stage3/prepare_dpo_data.py`从Stage 2输出的JSONL偏好数据转换而来。每条训练样本为一个三元组：

```json
{
  "prompt": "<|im_start|>user\n问题文本<|im_end|>\n<|im_start|>assistant\n已生成的推理前缀",
  "chosen": "偏好的后续推理步骤",
  "rejected": "不偏好的后续推理步骤"
}
```

其中prompt包含原始问题和到当前决策点为止的推理链前缀，chosen为搜索树中成功路径上的后继步骤，rejected为同层被剪枝的评分最低兄弟节点的步骤。这种prompt构造方式使模型在训练时学习的是"在特定推理上下文下，如何选择下一步推理方向"，而非从头生成完整推理链。

### 5.2.2 DPO损失函数实现

CPO训练使用第2.5节介绍的DPO损失函数，由`src/cpo/train/dpo_trainer.py`实现。关键实现细节如下：

**参考模型**：使用冻结的基座模型作为π_ref。在训练开始时，对π_ref进行一次完整的推理缓存，避免每步都重新计算参考模型的对数概率，显著提升训练速度。

**对数概率计算**：对于每个(prompt, response)对，将prompt和response拼接为完整序列，计算模型在response部分每个token的对数概率，并对序列长度进行归一化。实现中通过attention_mask区分prompt部分和response部分，前者不参与损失计算。

**β参数控制**：DPO损失中的β参数控制策略模型偏离参考模型的程度。根据CPO论文的建议和初步实验，默认设置β=0.1。β过大导致优化信号过弱（模型变化不足），β过小则可能过度偏离导致语言能力退化。

### 5.2.3 训练配置

CPO训练的核心超参数配置如下：

| 参数 | 默认值 | 说明 |
|-----|--------|------|
| LoRA秩r | 16 | 低秩分解秩 |
| LoRA α | 32 | 缩放因子 |
| DPO β | 0.1 | 偏离惩罚系数 |
| 学习率 | 5e-5 | AdamW优化器 |
| Batch大小 | 4 | 每步训练样本数 |
| 训练轮数 | 3 | 全量数据遍历次数 |
| 最大序列长度 | 2048 | token截断长度 |
| 梯度累积步数 | 4 | 等效batch=16 |

训练过程使用`src/cpo/train/callbacks.py`进行训练监控，记录DPO损失、chosen/rejected对数概率比等指标，用于评估训练收敛情况和偏好信号的吸收程度。

---

## 5.3 TS-SFT训练路线

TS-SFT（Teacher-Student Supervised Fine-Tuning）作为对比基线，由`scripts/stage3/train_ts_sft.py`实现。其训练理念直接明了：将从ToT搜索中回溯的成功推理路径作为"教师"输出，让基座模型通过标准交叉熵损失直接模仿这些高质量推理链。

TS-SFT的训练数据格式为简单的(prompt, response)对：

```json
{
  "prompt": "问题文本",
  "response": "搜索成功路径上的完整推理链+最终答案"
}
```

与CPO不同，TS-SFT不区分步骤级的偏好信号——它将整条成功推理链作为一个整体进行模仿学习。这既简化了数据构造，但也丢失了步骤级的细粒度指导。

训练使用标准的自回归语言建模损失（交叉熵），仅在response部分计算损失而屏蔽prompt部分的梯度。训练超参数（学习率、batch大小、LoRA配置等）与CPO保持一致，确保对比公平。

---

## 5.4 推理与延迟测量

### 5.4.1 统一推理接口

训练完成后，CPO模型和TS-SFT模型均通过统一的推理接口进行评估，由`src/cpo/eval/inference.py`提供。推理流程为：

1. **模型加载**：加载基座模型，合并LoRA适配器权重。
2. **提示构建**：使用与训练一致的prompt格式。
3. **自回归生成**：使用CoT方式进行单次推理，采用与标准CoT基线相同的解码参数（温度、top_p等）。
4. **答案提取**：从生成文本中解析最终答案。

关键设计是推理时不进行任何树搜索——模型仅执行一次标准的CoT解码。这是CPO范式"离线搜索、在线高效"核心优势的体现。

### 5.4.2 延迟测量方案

推理延迟由`src/cpo/eval/latency.py`统一测量，包含以下组成部分：

- **模型加载时间**：加载权重和LoRA适配器的时间（测量一次）。
- **单问题推理时间**：从prompt输入到完整推理链输出的端到端时间。
- **token生成速率**：输出token数除以解码时间。

延迟测量使用wall-clock时间，重复多次取平均以消除系统波动影响。对比各方法的延迟时，使用相同的硬件环境和推理引擎（HuggingFace Transformers 直接调用 `model.generate()`），保证公平性。

### 5.4.3 SC-ToT基线推理

SC-ToT作为搜索基线，在评估阶段调用`scripts/stage4/run_tot_eval_only.py`。为控制对比变量，评估模式支持复用Stage 2已完成搜索的树结构（避免重复搜索），仅重新执行终端自洽性投票和答案聚合。

---

## 5.5 训练监控与回调机制

`src/cpo/train/callbacks.py`实现了训练过程的监控回调。在DPO训练中，监控指标包括：

- **DPO Loss**：主损失函数值，期望随训练下降并趋于平稳。
- **Chosen对数概率**：模型对偏好步骤的生成概率，期望上升。
- **Rejected对数概率**：模型对不偏好步骤的生成概率，期望下降。
- **概率差值**：chosen_logprob - rejected_logprob，衡量模型区分偏好信号的能力，期望增大。
- **KL散度**：π_θ与π_ref之间的KL散度（通过DPO损失项中隐含的关系间接监控），过大表示模型过度偏离。

在TS-SFT训练中，监控指标为标准交叉熵损失和token级准确率。所有监控指标通过日志记录，供Stage 5可视化使用。

---

> **第五章合计约2,300字。** 累计：2,600 + 2,800 + 2,300 + 2,900 + 2,300 = 12,900字。
